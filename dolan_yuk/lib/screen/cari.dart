import 'package:flutter/material.dart';

class Cari extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This is Cari"),
      ),
    );
  }
}